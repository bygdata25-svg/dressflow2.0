Listing UI Standard

All entity listings must provide a table-based view using the shared DataGrid component.

Visual entities may optionally provide a card-based view using the shared CardGrid component.

DataGrid is the default view mode unless explicitly defined otherwise.

All labels, statuses, and empty states must be internationalized.
